type User {
  id: String!
  firstName: String!
  lastName: String!
  email: String!
}

type Query {
  me: User
}

type Mutation {
  signin(email: String!, password: String!): String
  signup(
    firstName: String!
    lastName: String!
    email: String!
    password: String!
  ): String
}
