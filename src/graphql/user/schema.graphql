type User {
  id: String!
  firstName: String!
  lastName: String!
  email: String!
}

type Query {
  me: User
}

type Mutation {
  signin(email: String!, password: String!): User
  signup(
    firstName: String!
    lastName: String!
    email: String!
    password: String!
  ): User
}
