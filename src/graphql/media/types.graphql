type Media {
  id: String
  userId: String
  name: String
  mediaType: String
  size: Int
  md5: String
  path: String
  url: String
}

type Query {
  media(id: String!): Media
}

type Mutation {
  addMediaFile(file: Upload!): Media
}
